Circular avatar — teacher/child photo or coloured initials.

```jsx
<Avatar name="Анна Петрова" color="purple" size="lg" />
<Avatar src="/teacher.jpg" name="Мария" ring />
```

Sizes `xs`→`xl`. Without `src`, shows initials on a pastel fill. `ring` adds a brand outline.
