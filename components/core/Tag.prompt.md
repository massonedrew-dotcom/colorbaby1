Category chip with a coloured dot — programme filters and tags.

```jsx
<Tag dot="var(--cat-english)">Английский</Tag>
<Tag dot="var(--cat-chess)" active onClick={...}>Шахматы</Tag>
```

Pair `dot` with the `--cat-*` palette. `active` fills the chip dark for the selected filter.
