# SEO чек-лист — Color Baby House

Разбор сайта по базовому SEO чек-листу (`SEO - чек-лист базовая проверка.xlsx`) и отчёт о том,
что уже сделано в коде.

**Дата:** 04.08.2026 · **Целевой хостинг:** Узбекистан, домен `colorbabyhouse.uz`
**Обозначения:** ✅ сделано · ⚠️ частично · ⛔ нужны данные или действия клиента · — не применимо

---

## Что изменилось в двух словах

Сайт перестал быть одной страницей, которая собиралась в браузере посетителя.

| | Было | Стало |
|---|---|---|
| Адрес главной | `/ui_kits/website/standalone.html` | `/` |
| Что видит поисковый робот | пустой `<div id="root">` | полный HTML с текстом (пререндер) |
| Вес скриптов | ~4,5 МБ (React dev + Babel 3 МБ с unpkg) | ~630 КБ, всё со своего домена |
| Копий главной в индексе | 3 | 1 |
| robots.txt / sitemap.xml / 404 | нет | есть |
| Заявки с форм | терялись | уходят на endpoint из конфига |
| Согласие на обработку ПД | текст под кнопкой | чекбокс, по умолчанию не отмечен |

Сборка: **`node tools/build.js`** — компилирует JSX и заново записывает пререндер в `index.html`.
Запускать после каждой правки `*.jsx`. Локальная проверка: `node tools/serve.js` → http://localhost:4173

---

## 1. Общее / внешние сервисы

| Пункт | Статус | Комментарий |
|---|---|---|
| Яндекс.Метрика на всех страницах | ✅ код / ⛔ номер | Загрузчик в `ui_kits/website/analytics.js`. Впишите номер в `METRIKA_ID` (`site.config.js`) — счётчик появится сам |
| Цели в Метрике (кнопки, формы) | ✅ | События уже отправляются: `lead_enroll`, `lead_quick`, `open_quick_apply`, `click_phone`, `click_telegram`, `click_instagram`, `click_enroll_header`, `click_enroll_faq`, `open_faq`. Остаётся создать цели с этими именами в интерфейсе Метрики |
| Google Analytics 4 | ✅ код / ⛔ номер | `GA4_ID` в `site.config.js` |
| Подтверждение в Яндекс.Вебмастере и GSC | ✅ место / ⛔ коды | Готовые строки в `index.html`, раскомментировать и вставить коды |
| Яндекс.Бизнес, Google Business Profile | ⛔ | Заводит клиент. Ссылки на карточки потом добавить в `sameAs` микроразметки |
| SSL, нет страниц с `http://` | ✅ | Принудительный редирект на https в `.htaccess` и `nginx.conf.example` |
| Дубли страниц | ✅ | `ui_kits/website/index.html` и `standalone.html` закрыты `noindex` + canonical на `/`, плюс 301 на сервере (legacy-экспорт `Homepage.html` удалён вместе со старым брендом) |
| Недоступность по IPv6 | ⛔ | Проверять на боевом хостинге: `nslookup colorbabyhouse.uz` |
| Топвизор | ⛔ | SEO-специалист |

---

## 2. Индексация и служебные файлы

| Пункт | Статус | Что сделано |
|---|---|---|
| robots.txt с отдельными правилами Яндекс/Google | ✅ | `robots.txt`: секции `Yandex`, `Googlebot`, `*`, `Clean-param` для utm-меток, `Host`, `Sitemap`. CSS и JS намеренно открыты — иначе робот увидит сломанную вёрстку |
| sitemap.xml | ✅ | `sitemap.xml` — главная, политика, согласие |
| Sitemap в вебмастерах | ⛔ | После открытия домена |
| 301 www → без www, http → https | ✅ | `.htaccess`, `nginx.conf.example` |
| 301 с `/index.html`, лишних слэшей | ✅ | Там же. Плюс 301 со старых адресов `/ui_kits/website/*` |
| Битые ссылки | ✅ | Пункты футера «Педагоги», «Питание», «Безопасность» вели на `href="#"` — заменены на реальные разделы (Галерея, Лето, Цены, Вопросы). Проверено: пустых ссылок 0 |
| Страница 404 с кодом 404 и оформлением | ✅ | `404.html` в стиле сайта: солнышко, ссылка на главную, телефон, быстрые ссылки. Код ответа даёт `ErrorDocument` / `error_page` |
| `rel="canonical"` на всех страницах | ✅ | Главная, политика, согласие, служебные страницы |
| ЧПУ | ✅ | `/`, `/privacy.html`, `/consent.html` |
| Копии сайта в индексе | ✅ | См. «Дубли» выше |

---

## 3. Мета-теги, заголовки, разметка

| Пункт | Статус | Что сделано |
|---|---|---|
| Title / Description | ✅ | Главная: «Частный садик в Ташкенте — Color Baby House, детский сад для детей 1–7 лет» с топонимом. Description с призывом. Свои title/description у политики и согласия |
| Дубли Title / Description | ✅ | Служебные страницы закрыты от индексации |
| H1 — один, иерархия H1→H2→H3 | ✅ | Проверено на живой странице: 1 × H1, 10 × H2, 28 × H3 |
| Фавикон (ico + svg + touch-icon) | ✅ | `favicon.ico` 32×32, векторный `favicon.svg`, `apple-touch-icon.png` 180×180, `icon-192/512.png`, `site.webmanifest`. Прежний фавикон весил 666 КБ, стало 3 КБ |
| Логотип — ссылка на главную (шапка и футер) | ✅ | `Header.jsx`, `Footer.jsx` |
| Микроразметка Schema.org | ✅ | `ChildCare` с двумя телефонами, адресом, часами (включая субботу), ссылкой на карты, тарифами в **сумах** (`hasOfferCatalog`), соцсетями. Плюс `WebSite` и `FAQPage` |
| Open Graph | ✅ | Абсолютные URL, обложка `assets/og-cover.jpg` 1200×630 с указанными размерами и alt |
| alt у картинок | ✅ | Все изображения с осмысленными alt; у занятий alt собирается из названия занятия |
| Вес картинок | ✅ | Все фото переведены в WebP (−45 % веса), логотипы пережаты: 666 КБ → 70 КБ |
| `width`/`height` у картинок (CLS) | ✅ | Проставлены всем: карта размеров в `Common.jsx`, атрибуты выдаёт `imgProps()`. Проверено: изображений без размеров — 0 |

---

## 4. Юзабилити и коммерческие факторы

| Пункт | Статус | Что сделано |
|---|---|---|
| Копирайт в подвале | ✅ / ⛔ год основания | Год подставляется автоматически. Заполните `FOUNDED` в `site.config.js` — появится «© 2015–2026» |
| Меню шапки | ✅ | Добавлен пункт «Вопросы»; порог перехода на бургер поднят до 1380 px, вёрстка проверена на 1381 и 1400 px — не ломается |
| Часы работы и адрес в подвале | ✅ | Полный адрес, оба телефона, часы работы в тегe `<address>` |
| Кнопка звонка на первом экране | ✅ | Кликабельный телефон в шапке (и в мобильном меню) — обычная `tel:`-ссылка, работает без JS |
| Кнопка «наверх» | ✅ | `Widgets.jsx`, появляется после полутора экранов, слева, чтобы не спорить с виджетом заявки |
| Соцсети | ✅ | Telegram, Instagram, телефон — настоящие ссылки вместо кнопок с `onClick` |
| Раздел «Отзывы» | ✅ | Был |
| FAQ | ✅ | Новый раздел: 10 вопросов, аккордеон + микроразметка `FAQPage` (шанс на расширенный сниппет). Тексты в `ui_kits/website/faq.data.js` — **проверьте их с клиентом** |
| Галерея | ✅ | `Gallery.jsx` был написан, но нигде не подключён — включён в страницу |
| Страница Контакты | ⚠️ | Карта — картинка-ссылка на Яндекс.Карты. Интерактивная карта и реквизиты юрлица — когда будут данные |
| E-mail на своём домене | ⛔ | Заведите ящик и впишите `EMAIL` в `site.config.js` — он сам появится в футере и микроразметке |
| WhatsApp | ⛔ | Не добавлял: неизвестно, отвечают ли там. Скажете — добавлю |
| Вакансии, блог | ⛔ | Нужен реальный контент, выдумывать нельзя |

---

## 5. Законодательство (Республика Узбекистан)

| Пункт | Статус | Что сделано |
|---|---|---|
| Политика конфиденциальности | ✅ | `privacy.html` перенесена в корень, дополнена: аналитика, размещение данных на территории РУз, права субъекта, срок хранения. Основание — Закон РУз «О персональных данных» № ЗРУ-547 |
| Страница «Согласие на обработку ПД» | ✅ | Новая `consent.html`: перечень данных, цели, действия, срок, отзыв |
| Ссылки на обе страницы в футере | ✅ | |
| Чекбокс согласия в формах | ✅ | `ConsentCheck` в `Common.jsx`. По умолчанию **не отмечен**, кнопка отправки заблокирована, пока не отметят — проверено на живой странице |
| Уведомление о cookie и аналитике | ✅ | Баннер в `Widgets.jsx`. Пока выбор не сделан, **счётчики не грузятся вовсе** |
| Валидация форм | ✅ | Телефон проверяется в обеих формах, Telegram — по формату; ошибки показываются под полем |
| Реквизиты оператора в документах | ⛔ | В обеих страницах стоит заметный блок-напоминание. Тексты — шаблон, нужна вычитка юристом |

---

## 6. Вёрстка, валидация, скорость

| Пункт | Статус | Что сделано |
|---|---|---|
| `type="tel"` + `inputmode` + маска | ✅ | Плюс `autocomplete="tel"` и `autocomplete="name"` |
| Поля e-mail | — | В формах их нет намеренно: лишнее поле снижает конверсию, связь идёт по телефону и Telegram |
| Адаптивность | ⚠️ | Медиазапросы на месте, шапка проверена. **Глазами на телефоне не смотрел** — окно предпросмотра в этой сессии не отрисовывало кадры |
| Скорость (Core Web Vitals) | ✅ | Babel из браузера убран совсем (JSX собирается заранее), React переведён на production-сборку, всё вынесено с unpkg на свой домен, добавлены preload шрифтов и баннера, gzip и кэш в конфигах сервера |
| Контент виден без JavaScript | ✅ | Пререндер: 142 КБ готовой разметки записывается в `index.html` при сборке. Плюс `<noscript>` с адресом и телефонами |

---

## Что осталось за клиентом

1. **Домен и хостинг в Узбекистане** — выложить сайт, включить SSL, проверить, что `.htaccess` подхватился.
2. **Счётчики** — номер Яндекс.Метрики и идентификатор GA4 в `site.config.js`.
3. **Приём заявок** — адрес обработчика в `FORM_ENDPOINT`. Пока пусто, форма показывает благодарность, но в консоль пишет предупреждение, что заявка никуда не ушла.
4. **Коды подтверждения** для Яндекс.Вебмастера и Google Search Console.
5. **Почта на домене** (`EMAIL`), **год основания** (`FOUNDED`).
6. **Вычитка юристом** политики и согласия, реквизиты юрлица.
7. **Проверка текстов FAQ** — я собрал их из того, что уже написано на сайте, но подтвердить должен садик.
8. Карточки в **Яндекс.Бизнесе** и **Google Business Profile**, ссылки на них — в `sameAs`.

Всё это правится в одном файле `site.config.js` — трогать код не нужно.
