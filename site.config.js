/* Color Baby House — единая точка настройки сайта.
   Здесь собрано всё, что меняется при запуске и переезде: домен, счётчики,
   приёмник заявок, контакты. Файл подключается ПЕРВЫМ, до всех остальных
   скриптов, и читается как разметкой, так и React-компонентами.

   ВАЖНО: пустая строка = функция выключена. Ничего не грузится, ошибок нет.
   Заполнили METRIKA_ID — счётчик появился; заполнили FORM_ENDPOINT — заявки
   начали уходить. Перевыкладывать больше ничего не нужно. */
window.CS_CONFIG = {
  /* ===== Домен ===== */
  /* Канонический адрес сайта, без слэша на конце. Отсюда строятся canonical,
     Open Graph и микроразметка. При смене домена правится ТОЛЬКО эта строка. */
  SITE_URL: 'https://colorbabyhouse.uz',

  /* ===== Аналитика ===== */
  /* Номер счётчика Яндекс.Метрики, например '99887766'. Берётся в metrika.yandex.ru */
  METRIKA_ID: '',
  /* Идентификатор потока Google Analytics 4, например 'G-XXXXXXXXXX' */
  GA4_ID: '',
  /* Счётчики грузятся только после согласия в cookie-баннере.
     false — грузить сразу, без ожидания согласия (не рекомендуется). */
  ANALYTICS_REQUIRES_CONSENT: true,

  /* ===== Подтверждение прав в вебмастерах ===== */
  /* Содержимое content="..." из мета-тега, который выдают панели вебмастеров */
  YANDEX_VERIFICATION: '',
  GOOGLE_VERIFICATION: '',

  /* ===== Приём заявок ===== */
  /* URL, на который уходит POST с заявкой (JSON: name, phone, telegram, age, page).
     Подойдёт любой обработчик на хостинге, например /api/lead.php.
     Пока пусто — форма показывает благодарность, но данные никуда не уходят,
     и в консоль пишется предупреждение. */
  FORM_ENDPOINT: '',

  /* ===== Контакты (используются в разметке и в микроразметке) ===== */
  PHONE_PRIMARY: '+998901766999',
  PHONE_SECONDARY: '+998946712626',
  TELEGRAM: 'colorbabyhouse',
  INSTAGRAM: 'colorbabyhouse',
  /* Почта на своём домене. Пусто — блок с почтой не показывается.
     Завести ящик после делегирования домена и вписать сюда. */
  EMAIL: '',
  ADDRESS: 'г. Ташкент, улица Тафаккур, 6',
  /* Ориентир — им люди находят садик быстрее, чем по номеру дома.
     Пусто — строка с ориентиром просто не показывается. */
  LANDMARK: 'сзади гостиницы «Россия»',
  /* Год основания — для копирайта в подвале. Пусто — показывается только текущий год. */
  FOUNDED: '',
};
